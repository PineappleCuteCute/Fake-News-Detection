function checkArticle() {
    var textInput = document.getElementById('textInput').value;
  
    // Gửi văn bản đến máy chủ để kiểm tra (bạn cần triển khai điều này)
    // Vì đơn giản, chỉ log văn bản đầu vào ra console trong ví dụ này
    console.log('Văn bản để kiểm tra:', textInput);
  }
  